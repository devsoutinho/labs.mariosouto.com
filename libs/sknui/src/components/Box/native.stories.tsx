import { Box } from "./native";

export const Component = Box;

export const controls = {
  props: {
    children: null,
    styleSheet: {
      width: 100,
      height: 100,
      backgroundColor: "#eee",
      padding: 10,
    },
  },
};
