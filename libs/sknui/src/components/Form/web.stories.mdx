import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Form } from "./web";

<Meta title="Forms/Form" />

# Form

Form is ...

<ArgsTable of={Form} />

<Canvas>
  <Story
    name="Form Component - Default"
    args={{
      ...Form.defaultProps,
      styleSheet: {
          backgroundColor: "orange",
          padding: "10px",
          width: "50px",
          height: "50px",
      }
    }}
>
    {(args) => {
      return <Form {...args} />;
    }}
  </Story>
</Canvas>
