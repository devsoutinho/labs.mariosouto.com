import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Text } from "./web";

<Meta title="Components/Text" />

# Text

Text is the base component for texts

<ArgsTable of={Text} />

<Canvas>
  <Story
    name="Text Component - Default"
    args={{
      ...Text.defaultProps,
      children: "Hello World",
    }}
>
    {(args) => {
      return <Text {...args} />;
    }}
  </Story>
</Canvas>


<Canvas>
  <Story
    name="Text Component - with responsive typography"
    args={{
      ...Text.defaultProps,
      typographyToken: {
        xs: "body1",
        sm: "display1"
      },
      children: "Hello World",
    }}
>
    {(args) => {
      return <Text {...args} />;
    }}
  </Story>
</Canvas>
