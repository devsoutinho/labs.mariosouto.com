import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { InputText } from "./web";

<Meta title="Components/InputText" />

# InputText

InputText is ...

<ArgsTable of={InputText} />

<Canvas>
  <Story
    name="InputText Component - Default"
    args={{
      ...InputText.defaultProps,
      styleSheet: {
          backgroundColor: "orange",
          padding: "10px",
          width: "50px",
          height: "50px",
      }
    }}
>
    {(args) => {
      return <InputText {...args} />;
    }}
  </Story>
</Canvas>
